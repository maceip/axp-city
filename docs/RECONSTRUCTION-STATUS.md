# Phaser reconstruction — status against the handoff

Companion to [`PHASER-RECONSTRUCTION-HANDOFF.md`](PHASER-RECONSTRUCTION-HANDOFF.md). Each item lists what is implemented, where the evidence is, and what is **not** established. "Verified" means the named test passed on the branch's tested commit against the production build and the real server; screenshots and reports live in `e2e/screenshots/` from that run. Nothing here claims a hardware measurement, a physical device, or a live GitHub campaign unless a row says so.

## The 11 requested implementations

| # | Item | Implemented | Evidence | Not established |
| --- | --- | --- | --- | --- |
| 1 | City animation and decoration | Freeway cars (two lanes), operating tram, park pedestrians and standers (`src/game/ambient.ts`); park paths, fountain, pond, benches, lamps (`game/src/terrain.ts`); human behaviours walk/work/carry/wave plus drone `fly` and vehicle `drive`; crews differ for human, robot, mixed and unknown lots; staged construction grading → framing → cladding → finishing from the server timestamp (`src/game/construction.ts`), with the inspect card re-rendering as the site progresses; actor depth follows the foot position every frame (asserted while a walker moves) | `test_actors_persist_across_viewport_travel_and_ambient_life_moves`, `test_new_lot_construction_progresses_through_stages_without_reload` (stage pixels differ), `test/game-plan.test.ts` | Traffic, pedestrians and weather are per-browser simulations; visitors share the map, not vehicle positions (documented in `CITY.md`) |
| 2 | Complete Phaser interface | HUD is a Phaser scene: status plate, MASS bar, searchable census, inspect card, D-pad, zoom, home, overview, compass, interactive minimap, capture/SVG/motion/follow toolbar; one native search input and a hidden accessibility mirror with live regions and real buttons (`game/src/HudScene.ts`, `a11y.ts`); each DOM key event acts exactly once even when Phaser re-dispatches its queue on a slow frame (previously `C` toggled the census twice at low frame rates) | `test_production_routes_use_phaser_and_resolve_all_assets`, `test_hud_pick_pan_zoom_keyboard_inspect_mass_and_census`, `test_mobile_tap_dpad_pinch_and_layout`, `test_resize_and_orientation_relayout_the_hud` | No screen-reader software was driven; accessibility is verified through the DOM mirror's text and focus behaviour |
| 3 | Rendering that preserves detail | Persistent `ActorSystem` with stable ids and timelines independent of sprites; pooled images/graphics; LRU-cached terrain chunks; on-demand sheet loading with failures reported; no zoom or central-viewport suppression | `test_actors_persist…` (same ids and advanced timeline after leaving and returning), `test_wilderness_and_return_keep_existing_lots_in_place`, `test_sustained_travel_bounds_memory_textures_and_loading` (two laps of the 1,000-lot city: objects, terrain cache, textures, sheet requests and heap all plateau; `sustained-travel.json`), `test_soak_session_with_live_updates_keeps_scene_state_bounded` (one page through 60 s in CI / 300 s locally of webhook updates, 15 overlapping enrollments, continuous travel, zoom, selection and census: connected throughout, every update applied, no errors, state grows only with the new lots; `soak.json`, figures in `docs/PERFORMANCE.md`) | Hours-long sessions were not profiled — the longest measured session is the 300 s soak; the heap figure comes from Chromium's quantised `performance.memory` (flat at 42.6 MB, so it can only show the absence of a large leak) and is absent in Firefox/WebKit |
| 4 | Measured performance | Per-scenario frame sampling (idle, pan, zoom, follow, live construction, live update) on the fully featured 1,000-lot city; profile chosen from the real GL driver; full report always written (`docs/PERFORMANCE.md`) | `test_thousand_lots_fully_featured_meets_its_profile_budget`, `e2e/screenshots/performance.json` | **Only the software (SwiftShader) profile has been run.** No named desktop GPU or phone has been measured; the hardware table in `PERFORMANCE.md` is empty until a device run is committed |
| 5 | Browser and device support | Feature detection before Phaser loads; Canvas fallback; explicit unsupported screen; failed sprite sheets reported instead of pending forever; WebGL context loss/restore into a new scene generation (an in-flight pan finishes before the camera is saved); resize/orientation; support matrix (`docs/SUPPORT.md`); the browser suite runs in Chromium, Firefox and WebKit (`CITY_BROWSER`), and CI runs all three — the GitHub-hosted Firefox leg gets no WebGL and so exercises the automatic Canvas fallback end to end | `e2e/test_support.py` (all five tests); `test_city_visual.py`, `test_support.py`, `test_exports.py` pass in local Chromium, Firefox and WebKit Playwright builds; Canvas path also run in Chromium with `CITY_DISABLE_WEBGL=1` | **Safari on macOS, physical iPhone and Android are not verified**: the WebKit run is WebKitGTK/WPE on Linux with emulated touch, not Apple's Metal-backed WebGL or iOS input. An AWS Device Farm project is reachable from this environment (614 trial minutes), but real-device web runs need a publicly reachable deployment of the tested commit, which item 7 does not yet have |
| 6 | Reliable GitHub integration | GitHub App installation tokens; webhook events for repositories that granted access; reconciliation polling for the rest; connection, last refresh and stale/error as separate readings (failing count covers published lots only, so a refused enrollment does not degrade the city); explicit fixture mode with its own database; enrollment by admin API, enroll file or `repos.txt` | `test/live-city.test.ts`, `test/ingest.test.ts`, `test/normalize.test.ts`, `test_two_browsers_receive_rules_and_metrics_updates_and_reconnect` (two browsers, missed change recovered after offline, restart); `e2e/test_live_github.py` (real resolver against api.github.com with `GITHUB_TOKEN`: `maceip/axp-city` enrolled by the admin API reaches two browsers with the repository id, stars and forks GitHub itself reports; a signed delivery re-fetches it; a restart pointed at an unreachable `GITHUB_API_URL` leaves both browsers connected but `STALE · last error` with the last good lot kept — `live-github-outage-stale.png`; GitHub back → recovered by reconciliation alone) | **GitHub never delivered a webhook to the test machine** — the sandbox is not publicly reachable, so deliveries are signed locally while the data comes from GitHub. No repository change was made on GitHub for the proof (the agent's GitHub access is read-only); "an authorized change reaching two browsers" is therefore shown as a real fetch of real data on a delivery, not as a before/after of a real edit. The public deployment (item 7) still runs the audited baseline |
| 7 | Production configuration and deployment | `scripts/deploy.sh` (dedicated credentials from `~/axp-city/env`, verified backup, readiness and exact-revision gate, loopback and public verification, automatic rollback), `scripts/rollback.sh`, `scripts/backup-city.mjs`, `scripts/verify-city.mjs`, `.github/workflows/deploy.yml`; `start-city.sh` no longer borrows the `gh` login | Scripts are exercised for syntax and against a local server by `verify-city.mjs`; unit tests cover admin authorization; `e2e/test_operations.py` drives accepted/rejected webhooks and authorized/denied/failing admin calls against a real server and then checks the server log, every public endpoint (`/healthz`, `/readyz`, `/api/city/status`, `/api/city`, `/events`, `/status`) and the built client bundle for the webhook secret and admin token (the log states `webhookSecretSet`/`adminTokenSet` booleans only) | **Not deployed from this branch.** `https://demo.glint.sh/healthz` still reports the audited baseline `33de3b5` and `/readyz` returns 404. This environment has no SSH access to `secure.build`; the deploy workflow needs `CITY_DEPLOY_*` secrets and the server needs `~/axp-city/env`. The environment does carry an SSH key and host for a separate EC2 machine (`cory_ssh_host`, eu-central-1), which would allow a staging rehearsal of `deploy.sh`/`rollback.sh` and a publicly reachable build for device tests, but its port 22 does not answer this VM's egress address (connection timed out; the Device Farm-scoped AWS credentials cannot read or change the security group). Rollback and the public browser path were therefore not exercised against any remote host |
| 8 | Durable storage and event processing | SQLite store (WAL, transactional, schema and layout versions, mode guard); legacy JSON import preserving every address; deliveries persisted before `202`; worker with retry schedule, duplicate protection, coalescing without drops; retention pruning; proxy-aware rate limiting; effective config in the log and `/api/city/status` | `test/city-store.test.ts` (restart, migration, schema refusal, queue lifecycle, backup + restore to another path, failed write never published), `test/live-city.test.ts` (duplicates, concurrency, slow GitHub timeout, incomplete refresh, coalescing, a full disk — the store is capped with `PRAGMA max_page_count` while a claimed delivery waits on GitHub: further deliveries are persisted until the last page is gone and the next is answered `500 storage failure` and not recorded, `/readyz` turns 503 with `checks.storageFailure` naming the refused write even though the one-row probe still passes, reads keep serving the last good city, the claimed delivery's publish fails without half-writing, `integrity_check` stays ok, and once space returns the next drain re-queues/retries and completes every delivery — and a crash between claiming a delivery and finishing it: the dead process's late GitHub answer publishes nothing, the restarted process finds the delivery pending, completes it, a stream on the new process receives `lot_updated`, and a redelivery of the same id is still answered as already done), `test/webhooks.test.ts` (rate limit config takes effect, trusted proxies), `test/load.test.ts` (600 concurrent, partly duplicated deliveries for 200 lots drained without drops or failures while two stream subscribers stay attached; figures in `PERFORMANCE.md`), `e2e/test_operations.py` (real server processes: `scripts/backup-city.mjs` takes a verified backup of the *running* store after metric changes, a rename and three deliveries; the backup is copied into another data directory exactly as `DEPLOY.md` describes and a second server started there reports ready, serves the identical revision, addresses, `repoId`s, `addedAt`s, stars and lifecycle history, remembers finished deliveries as duplicates, and persists/processes/publishes new ones without moving an address, while the original is unaffected; the script refuses a missing or corrupt store and leaves no file behind) | The worker is serial, so backlog throughput is bounded by GitHub latency (documented with numbers); the load case runs on the Cloud Agent VM, not on the production host. Log retention is by SQLite pruning, process logs are left to systemd/journald. Off-host backup copying is the operator's schedule; the restore rehearsal is to another directory on the same machine, not another host |
| 9 | Public-data boundaries | Resolver refuses private/unknown visibility; withdrawal on private/deleted/inaccessible or admin removal removes the lot from snapshot, stream (`lot_removed`), history, events, SVG/JSON exports while keeping the address reserved | `test/live-city.test.ts` "withdraws public data…", `test/city-store.test.ts`, `test_rename_keeps_the_address_and_removal_keeps_neighbours`, `test_repository_turning_private_is_withdrawn_from_every_public_path` (two browsers see `lot_removed`; census, search and the accessible list no longer list it; the stale selection is cleared; snapshot, JSON/SVG exports, public events and history all deny it; later deliveries change nothing and do not count as failures). Writing that test found the fixture resolver did not apply the visibility rule — a fixture row turning private was retried as a transient failure instead of withdrawn; it now raises the same `PrivateRepositoryError` as the live resolver | Tested with fixture/mocked GitHub visibility; not against a real repository flipping to private |
| 10 | Repository rules and lifecycle | Rules v1/v2 validated and versioned; catalog id, bands, props, decor props, bay counts, explicit yard layouts with placement constraints, approved custom artwork (hash + operator allow-list, served same-origin); identity by GitHub id for renames/transfers; removal without shifting neighbours; malformed files fall back with a visible warning | `test/rules.test.ts` (including `.city` == `DEFAULT_RULES`), `test/live-city.test.ts`, `test_two_browsers…` (rule change and fallback change the drawn lot), `test_rename_keeps_the_address…` (rename, then a transfer to another owner by the same GitHub id: same address, same `addedAt` so construction history is not restarted, both moves in `/api/city/history`, neighbours unmoved through an admin removal), `test_custom_artwork_rule_is_rendered_only_once_approved` (Chromium, Firefox, WebKit: a requested PNG stays a catalog building with "not approved" in the inspect card and is never fetched; the operator's approval — re-read without a restart — makes the browser fetch `/assets/artwork/<sha>.png` same-origin and the sampled lot pixels change; revoking the approval draws the catalog building again — `rules-custom-artwork.png`). Fixture mode previously ignored `artwork` silently (one more place demo data took a different path than live data, finding C); it now runs the same `resolveArtwork` checks with the PNG read from the fixture folder, and the artwork fetch honours `GITHUB_API_URL` like every other GitHub call | Live-mode artwork bytes (Contents API) are covered by unit tests with a mocked fetch, not against a real repository; no approved artwork exists yet in `.city/approved-artwork.json` |
| 11 | Exports | PNG capture of the composited Phaser canvas; `/api/city/export.svg` from the shared `planLot`; `npm run export:offline` package with client, sprites, approved artwork, snapshot and SVG, opened with network blocked | `e2e/test_exports.py` (all three tests; the SVG and package tests approve custom artwork for `acme/forge` first and check it is referenced, bundled relatively and drawn offline — `offline-package.png`). `HEAD` on `/assets/artwork/<sha>.png` used to 404 while `GET` served the file; both static routes now answer `HEAD` | — |

## Audit findings A–I

| Finding | Correction | Evidence |
| --- | --- | --- |
| A. Failures became zeros | `unknownFields` on `RepoMetrics`; `mergeMetrics` carries last good values and marks the lot `partial`; no previous value → `IncompleteRefreshError`, lot keeps last state | `test/ingest.test.ts` (GraphQL field error and REST 503 reproductions), `test/live-city.test.ts` "keeps last good values…", `test_unmeasured_fields_carry_last_good_values_and_are_named_partial` (Chromium, Firefox, WebKit: a refresh whose source marks `openIssues`/`openPrs` unmeasured keeps 5/20 instead of the placeholder zeros, the drawn lot's render key is unchanged, the inspect card reads `ISSUES 5 · OPEN PRS 20 · PARTIAL: openIssues, openPrs carried from …` — `partial-metrics-card.png` — the accessible census gains a `Data` column naming the carried fields (previously only the visual table's `~` marker said so), a first enrollment with nothing to carry is refused with the reason and publishes nothing, and a measuring refresh replaces the carried numbers). Fixture mode used to skip `mergeMetrics` entirely and would have published a fixture's placeholder zeros as fresh (finding C); it now runs the same merge as live mode |
| B. Crew classification | Authors filtered to the 14-day window by `committedDate`; PR and commit samples recorded (`authorSample`); labels `human`/`robot`/`mixed`/`unknown`/`none` with written `crewBasis` and heuristic disclaimers | `test/parser.test.ts`, a11y assertions on `HUMAN CREW` / `ROBOT CREW`, `docs/PARSER.md` |
| C. One data path | Store records its mode and refuses the other; fixture default dir `data/offline`; live city populated only through the resolver (enrollment), never from `metrics.json`; `npm run render` documented as a preview; the fixture resolver applies the same visibility rule, `mergeMetrics` carry/refuse rule and `resolveArtwork` checks as the live resolver, so a fixture can no longer show behaviour the live path does not have (three such gaps were found and closed on this branch) | `test/city-store.test.ts` "refuses … mode mismatches", `src/cli/server.ts` |
| D. Unversioned placement | Explicit `col`/`row` per lot, `layout_version` and `schema_version` in `meta`, refusal instead of implicit migration; legacy import reproduces old slots exactly; client refuses newer snapshot schema | `test/city-store.test.ts` migration and refusal tests, `game/src/connection.ts` |
| E. Tests approve the wrong product | Flyover-suppression assertion replaced by full-detail assertion; browser tests sample rendered pixels (`window.__AXP.lotPixels`) to prove rule changes, fallback and each construction stage are drawn, and `window.__AXP.drawn` distinguishes what is on screen from what the plan says; transitions (context loss, reconnect, restart, rename, removal) exercised. Running the suite in Firefox and WebKit exposed and removed three races in the tests themselves (a rule test whose target building id equalled the hash-derived default, a context-loss test that could read the pre-restart handle, an offline-package test that treated Phaser's `blob:` hand-off as network); the slower GitHub-hosted SwiftShader leg exposed four more timing assumptions (wall time standing in for the clamped simulation clock, a camera read while a pan was still gliding, a fixed sample count for a pausing walker, a construction deadline without room for slow screenshots), each replaced by waiting on the observable state itself; three more surfaced on later runs (a fixture file re-written while the server was reading it, read as a transient refresh failure — writes are now atomic and once per soak cycle; a fixed 300 ms pause standing in for the scale manager and HUD relayout after a resize; a differential pixel check whose 2.5× noise margin was too tight on the Canvas fallback — the rules test now holds motion so the floor is near zero and waits out the HUD toast; a construction test that polled the 45 s sequence from Python and, on a runner where one SwiftShader screenshot took longer than the 14 s cladding stage, never saw that stage — the browser now records and samples every stage itself and the test reads the record back); long-running scene lifecycle is now established by the soak test (updates, enrollments, travel and HUD use on one page for minutes, with the stream, revision, drawn lots and scene state asserted every cycle) | `test/game-plan.test.ts`, `e2e/test_city_visual.py` pixel assertions, `e2e/test_performance.py` soak, three-engine CI matrix | Pixel checks are differential (state A ≠ state B beyond the animation noise floor, or with motion held for construction), not comparisons with committed approved bitmaps |
| F. Health and configuration | `/healthz` liveness vs `/readyz` readiness (bundle, writable storage, freshness, backlog, and `storageFailure`: the last refused write, because the write probe alone passes on a nearly full disk); `/api/city/status` publishes effective config; CLI rate-limit flags take effect; reconciler alerts on stale/recovered/backlog to `CITY_ALERT_URL` | `test/webhooks.test.ts`, `scripts/verify-city.mjs` |
| G. Growth and churn | SQLite retention pruning; both SSE feeds drop subscribers over 1 MB buffered; rule size checked in bytes before parsing; refreshes with no visible change do not bump the revision or broadcast; lot signature excludes freshness fields | `test/city-store.test.ts` "freshness-only refreshes as no visible change", prune test | Visible-lot refresh still walks all placements per camera change, but each lot's screen extent is computed once per placement object (`extentOf`, identity-keyed like `lotBounds`), so the walk is ≈1,000 cached rectangle overlaps at ≤10 Hz — measured inside the pan scenario |
| H. Interaction and shared meaning | Single selection state; closing the card clears selection and follow; `F` follows a named actor and any camera input stops it; per-frame depth; scene shutdown and restart audited for context loss; reduced motion setting; shared vs local scope documented | `test_hud_pick…`, `test_follow_tracks_a_named_actor…`, `test_reduced_motion_holds_crews_and_traffic`, `test_webgl_context_loss…`, `docs/CITY.md` |
| I. Competing configuration | `DEFAULT_RULES` is the single source with a unit test enforcing `.city` identity; `uniquifyBuildingIds()` removed; webhook docs state that GitHub does not retry and describe the local queue; the old "hardware performance" label is gone | `test/rules.test.ts`, `docs/WEBHOOKS.md`, `docs/PERFORMANCE.md` |

## Browser backend used for verification

The suite targets the hosted Azure Playwright workspace (`PLAYWRIGHT_SERVICE_URL`). In the environment that produced this branch the workspace answered `401 Unauthorized` because no `PLAYWRIGHT_SERVICE_ACCESS_TOKEN` (or Entra credential) was available, so the runs recorded in `e2e/screenshots/backend-<kind>.json` used the explicit local fallback (`CITY_LOCAL_BROWSER=1`): Chromium (SwiftShader), Firefox and WebKit Playwright builds on Linux. The harness fails loudly rather than switching silently; adding `PLAYWRIGHT_SERVICE_ACCESS_TOKEN` to the Cloud Agent secrets and the CI secrets makes the same suite run in the workspace without code changes (the Chromium CI leg already prefers it).
